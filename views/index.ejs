<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <link rel="stylesheet" href="/public/index.css" type="text/css">
    <title>Chitter Page</title>
</head>
    <body>  
        <header>
            <h1 class="header_item">Chitter in Javascript</h1>
        </header>

        <% result.reverse().forEach(item => { %>
            <p><%= item.message %></p> 
            <p><%= item.message_time.toString().split(' ').slice(2, 5).join('-') %></p> 
            <form action='/deleteChitter/<%= item.message_id %>' method="GET"> 
                <input type="submit" value="Delete">
            </form>
        <% }) %>

    </body>
</html>